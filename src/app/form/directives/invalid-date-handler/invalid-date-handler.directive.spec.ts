import { InvalidDateHandlerDirective } from "./invalid-date-handler.directive";

describe("InvalidDateHandlerDirective", () => {
  it("should create an instance", () => {
    const directive = new InvalidDateHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
